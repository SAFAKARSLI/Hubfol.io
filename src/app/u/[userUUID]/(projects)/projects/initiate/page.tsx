import ProjectForm from '@/components/project-form/ProjectForm';
import { SlugProps } from '@/types/slug';
import React from 'react';

export default function page({}: SlugProps) {
  return <ProjectForm />;
}
